{
  "$comment": [
    "Advisories that are known, judged, and deliberately not fixed yet.",
    "",
    "`npm audit` has no allowlist, so the choice it leaves you is to fail on",
    "everything or gate on nothing. Both are useless: a gate that cannot",
    "express 'we looked at this one' gets switched off the first time it is",
    "inconvenient, and then it stops catching the advisories that DO matter.",
    "",
    "scripts/audit-gate.mjs fails on anything not listed here. Adding an entry",
    "is a decision with a reason attached, not a way to make a run go green.",
    "Never add one without reading the advisory and writing down why."
  ],
  "allowed": [
    {
      "package": "tar",
      "advisories": ["GHSA-34x7-hfp2-rc4v", "GHSA-8qq5-rm4j-mr97", "GHSA-83g3-92jg-28cx", "GHSA-qffp-2rhf-9h96", "GHSA-9ppj-qmqm-q256", "GHSA-r6q2-hw4h-h46w", "GHSA-vmf3-w455-68vh", "GHSA-w8wr-v893-vjvp", "GHSA-23hp-3jrh-7fpw", "GHSA-8x88-c5mf-7j5w", "GHSA-gvwx-54wh-qm9j", "GHSA-r292-9mhp-454m"],
      "severity": "critical",
      "reason": "Reached only through @expo/cli, which declares tar ^6.2.1 and uses it to unpack the Expo project template at build time. It never runs on a user's device and never sees an archive from anywhere but the npm registry.",
      "whyNotFixed": "There is no patched 6.x. Forcing tar 7 through overrides was tried on 2026-08-06 and BROKE THE NATIVE BUILD: tar 7 sets __esModule without a default export, so @expo/cli's _interopRequireDefault yields undefined and prebuild dies with \"Cannot read properties of undefined (reading 'extract')\". The audit went green while the app could no longer be built — a worse outcome than the advisory.",
      "realFix": "Expo 52 → 57, which ships a CLI that uses tar 7 correctly. Tracked separately; it is a five-major migration with a React Native upgrade underneath and is not an audit fix.",
      "addedOn": "2026-08-06"
    },
    {
      "package": "cacache",
      "advisories": ["*"],
      "severity": "high",
      "reason": "Flagged only because it depends on the tar above. Same path, same reasoning.",
      "whyNotFixed": "Resolves when tar does.",
      "realFix": "Expo 52 → 57.",
      "addedOn": "2026-08-06"
    },
    {
      "package": "@expo/cli",
      "advisories": ["*"],
      "severity": "high",
      "reason": "Flagged only because it depends on cacache and tar above. Build-time CLI, not shipped.",
      "whyNotFixed": "Resolves when tar does.",
      "realFix": "Expo 52 → 57.",
      "addedOn": "2026-08-06"
    },
    {
      "package": "expo",
      "advisories": ["*"],
      "severity": "high",
      "reason": "Flagged only because it depends on @expo/cli above.",
      "whyNotFixed": "Resolves when tar does.",
      "realFix": "Expo 52 → 57.",
      "addedOn": "2026-08-06"
    }
  ]
}
